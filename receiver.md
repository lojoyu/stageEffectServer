# 舞台效果控制器 - 接收端 (Receiver) 開發指南

本文檔旨在為接收端 (Receiver) 的開發者提供與舞台效果伺服器互動的指南。接收端的主要職責是監聽來自伺服器的指令，並根據這些指令執行相應的舞台效果，例如燈光變化、播放語音等。

## 連接伺服器

接收端需要通過 Socket.IO 連接到伺服器的 `/receiver` 命名空間。

**伺服器端點**: `[您的伺服器地址]/receiver` (例如 `ws://localhost:8000/receiver`)

## 監聽來自伺服器的事件 (Server -> Receiver)

接收端應監聽以下來自伺服器的 Socket.IO 事件：

### 1. `controlData`

-   **事件名**: `controlData`
-   **目的**: 接收主要的舞台效果指令。
-   **數據結構**:
    ```json
    {
      // 具體的效果參數，結構取決於控制端 (Controller) 的發送和接收端的實現
      // 例如：
      // "color": "red",
      // "intensity": 0.8,
      // "pattern": "blink"
    }
    ```
-   **接收端行為**: 根據接收到的 `data` 內容執行相應的舞台效果。例如，改變燈光顏色、播放特定音效、觸發動畫等。

### 2. `speak`

-   **事件名**: `speak`
-   **目的**: 接收播放語音的指令。
-   **數據結構**:
    ```json
    {
      "id": "unique_sentence_id", // 當前句子的唯一標識符
      "text": "要播放的文本內容。",    // 需要進行語音合成的文本
      "rate": 1.0,                // (可選) 語速 (預設 1.0)
      "pitch": 1.0,               // (可選) 音高 (預設 1.0)
      "volume": 1.0,              // (可選) 音量 (預設 1.0)
      "voice": "Google 普通话（中国大陆）" // (可選) 建議使用的語音引擎/名稱
    }
    ```
-   **接收端行為**:
    -   使用內建或外部的語音合成 (TTS) 功能，根據提供的 `text`、`rate`、`pitch`、`volume` 和 `voice` 參數播放語音。
    -   **重要**: 語音播放完成後，必須向伺服器發送 `speakOver` 事件，並附帶相同的 `id`。

### 3. `speakConfig`

-   **事件名**: `speakConfig`
-   **目的**: 接收來自伺服器的全域語音配置更新（通常由控制端觸發並廣播）。
-   **數據結構 (示例)**:
    ```json
    {
      "mode": "changeVoice", // 配置模式，例如更改預設語音
      "voice": "新的預設語音名稱"
      // ... 其他可能的配置參數
    }
    ```
-   **接收端行為**: 根據接收到的配置更新自身的語音相關設定。例如，如果 `mode` 是 `changeVoice`，則更改後續語音播放時使用的預設語音。

### 4. `debug`

-   **事件名**: `debug`
-   **目的**: 接收來自伺服器的調試信息。
-   **數據**: 通常是字符串。
-   **接收端行為**: 可以將此信息打印到控制台或用於其他調試目的。

## 向伺服器發送事件 (Receiver -> Server)

接收端需要在特定時機向伺服器發送以下 Socket.IO 事件：

### 1. `connected`

-   **事件名**: `connected`
-   **目的**: 在成功連接到 `/receiver` 命名空間後，立即向伺服器註冊自身並提供一個唯一標識符。
-   **數據結構**:
    ```json
    {
      "uuid": "your_receiver_unique_identifier" // 一個持久的、唯一的接收端標識符
    }
    ```
-   **發送時機**: 成功連接到 Socket.IO 伺服器的 `/receiver` 命名空間後，僅發送一次。
-   **重要性**: `uuid` 應該是持久的。如果接收端重啟或重新連接，使用相同的 `uuid` 可以幫助伺服器識別它。伺服器使用此 `uuid` 來映射到內部索引。

### 2. `speakConfig`

-   **事件名**: `speakConfig`
-   **目的**: （可選）向伺服器報告接收端自身的語音配置偏好，例如選擇了哪種語音。
-   **數據結構 (示例)**:
    ```json
    {
      "mode": "changeVoice", // 表示正在報告語音選擇
      "voice": "我選擇的語音：Microsoft Huihui Desktop - Chinese (Simplified)"
    }
    ```
-   **發送時機**: 當接收端的語音配置發生變化時（例如用戶在接收端界面上選擇了不同的語音），或者在初始化時報告預設配置。
-   **伺服器行為**: 伺服器會記錄此接收端 (`socket.id`) 對應的語音選擇 (`socketToVoice`)。

### 3. `speakOver`

-   **事件名**: `speakOver`
-   **目的**: 通知伺服器，先前通過 `speak` 事件接收到的指定語音片段已播放完畢。
-   **數據結構**:
    ```json
    {
      "id": "unique_sentence_id_that_finished" // 必須與收到的 `speak` 事件中的 `id` 相同
    }
    ```
-   **發送時機**: 在接收端完成播放由 `speak` 事件指定的句子後。
-   **伺服器行為**: 伺服器會根據此回調來判斷是否所有被指定的接收端都完成了當前句子的播放，然後決定是否觸發下一句或通知控制端整個語音任務已完成。

### 4. `debug`

-   **事件名**: `debug`
-   **目的**: （可選）向伺服器發送接收端的調試信息。
-   **數據結構**: 任意字符串或 JSON 對象。
-   **發送時機**: 在需要向伺服器報告調試狀態時。
-   **伺服器行為**: 伺服器可能會將此信息記錄下來，或在某些情況下回傳給該接收端或其他客戶端。

## 重要注意事項

-   **UUID 持久性**: 強烈建議為每個接收端設備或實例生成一個唯一的 `uuid`，並在多次連接中保持不變。這可以通過本地存儲（如 `localStorage`）等方式實現。
-   **效果實現**: 伺服器通過 `controlData` 事件發送的具體效果數據 (`data` 字段) 的格式和含義，需要與控制端和接收端之間提前約定。接收端需要根據這些約定來解析數據並實現相應的視覺、聽覺或其他效果。
-   **語音合成 (TTS)**: 接收端需要自行實現或集成 TTS 功能。伺服器僅傳遞文本和相關參數。
-   **錯誤處理與超時**: 雖然本文檔未詳細說明，但在實際開發中，接收端應考慮處理網絡錯誤、指令解析錯誤以及實現適當的超時邏輯（例如，如果長時間未收到伺服器指令）。

```